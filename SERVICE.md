# SERVICE · 학습지 생성 Agent

## 1. 사용자·문제·가치
- 누구를 위한 서비스인가: 초등 1~6학년 자녀를 둔 부모, 가정용 (입력 시 학년을 선택할 수 있음, 현재는 자녀 1명 기준, 추후 여러 자녀/학생으로 확장 가능성 있음. 과목마다 지원 학년 범위가 다를 수 있음 — 예: 수학·국어는 1~6학년, 영어는 3~6학년만 지원)
- 어떤 문제를 푸는가: 아이 수준에 맞는 수학·영어·국어·한문 학습지를 선생님 방문 일정에 얽매이지 않고 원할 때 받고 싶다
- 지금은 어떻게 해결하나: 주 1회 방문하는 구몬/빨간펜으로 하루 2장씩 학습지를 받아 푼다
- 이걸로 뭐가 좋아지나: 학년·과목만 입력하면 그날 분량(장당 10문제 내외, 하루 1~2장 = 10~20문제)이 자동 생성되고, 아이가 화면에서 풀면 바로 채점까지 된다
- 로드맵: 1차는 수학부터 시작해 검증한 뒤 영어·국어·한문으로 확장한다

## 2. 서비스 확장 관점
- 가정 내 기여: 방문 일정에 맞출 필요 없이 원하는 때 학습지를 받고, 채점까지 자동화해 부모의 관리 부담을 줄인다
- 규모: 가정 1곳, 자녀 1명, 주 5일 이상 사용 (추후 여러 자녀/학생으로 확장 가능성)
- 로드맵: 1차는 수학 한 과목만으로 핵심 루프를 검증하고, 이후 영어·국어·한문으로 확장한다
- 대체제와 차별점: 구몬/빨간펜은 방문 일정에 얽매이고 개인 맞춤 조정이 느리다 — 이 Agent는 원할 때 즉시 생성하고, 자동 채점과 난이도 조정까지 이어진다

## 3. 사용 예상 도구·데이터
- 도구 (1차, 수학 — 구현 완료): 수학 문제 생성(문제마다 짧은 주제 태그 포함, 예: "두 자리 수 뺄셈(받아내림)"), 오류 교차검증(생성된 모든 문제를 배포 전 다른 AI로 전수 검증, 실패 시 다른 문제로 자동 교체), 화면 풀이 채점, 점수(별) 보상(세부 산정 기준은 4번 참고 — 구현 완료 2026-09-17), 주간 리포트(결과 확인·날짜별 정답률 그래프·오답 주제 목록·난이도 조정 질문)
- 도구 (2차, 영어 확장 — 구현 완료 2026-09-17): 영어 문장 빈칸 채우기 문제 생성(4지선다형, 3~6학년 × 난이도별 — 1~2학년은 정규 영어 교과과정이 없어 제외). 빈칸은 어휘·문법만 다루고 철자는 제외한다. 화면 지시문은 한국어, 문장·보기는 영어로 낸다. 수학과 같은 생성→오류 교차검증→채점 파이프라인, 힌트·문제은행·HITL 대체 구조를 그대로 재사용한다(세부 정책은 4번 참고). 화면 풀이 채점(객관식 선택), 주간 리포트에 통합. 연습장(그림판) 기능은 필요 없다(4지선다라 풀이 낙서가 필요 없음, 수학 전용으로 유지)
- 주간 리포트는 과목이 늘어나면(영어 등) 상단에 과목별 요약(사용일수·정답률·완료한 학습지 수·힌트 사용률)을 보여주는 overview 섹션을 두고, 그 아래 상세(날짜별 정답률 그래프·오답 주제 목록 등)는 과목별로 나눠서 보여준다
- 도구 (오답노트, 구현 완료 2026-09-17): 채점 결과가 오답일 때마다 과목별 오답노트에 한 줄씩 기록한다(항목: 과목·학년·날짜·문제·보기·정답·아이가 쓴 답·태그). 세부 정책은 4번 참고
- 도구 (국어 확장 — 구현 완료 2026-09-18): 국어는 수학과 같은 1~6학년 전체를 지원한다(전 학년 공통 교과라서 영어처럼 학년을 제외하지 않는다). 학습지 한 세트(10문제) 중 마지막 1문제만 단답형(반대말·비슷한말 쓰기, 또는 맞춤법에 맞게 고쳐 쓰기 중 하나)이고 나머지 9문제는 4지선다 객관식(맞춤법·어휘·문법·띄어쓰기·높임말 등)이다. 화면 지시문·문제 모두 한국어. 수학·영어와 같은 생성→오류 교차검증→채점 파이프라인, 힌트·문제은행·HITL 대체 구조를 그대로 재사용한다(세부 정책은 4번 참고). 연습장(그림판) 기능은 필요 없다(수학 전용으로 유지)
- 도구 (후순위): 인쇄용 출력(연습용, 채점 없음), 카카오톡 알림, 한문 확장
- 데이터: 수학은 무료로 쓸 수 있는 MCP/API 제공처를 프로젝트 설정 단계에서 한 번만 확인해 고정하고(매 생성 시 재확인하지 않음), 없으면 AI가 학교 교과과정 기준으로 자체 생성 / 한자는 한국어문회 급수 기준(8급~) / 영어·국어는 학교 교과과정 기준으로 AI가 자체 판단해 생성한다(수학과 동일한 방식, 별도 DB 없음). 향후 RAG로 확장할 가능성이 있으나 전환 조건은 아직 정하지 않았다 — 로드맵 문구로만 남겨둔다. 어휘·문법 범위의 세부 데이터 출처는 착수 시 별도로 정한다

## 4. 서비스 정책 (가드레일 요약)
- 처음엔 자동채점 가능한 객관식/단답형 문제만 낸다 (서술형, 한자 쓰기, 영어 문장 작문은 범위 밖). 영어는 단답형이 아니라 4지선다형 문장 빈칸 채우기로 통일한다 (수학은 단답형·객관식을 섞어 쓰지만, 영어는 표기 차이로 인한 오채점 여지를 없애기 위해 객관식만 사용). 국어는 한 세트(10문제)당 단답형을 정확히 1문제(반대말·비슷한말 쓰기, 또는 맞춤법에 맞게 고쳐 쓰기 중 하나)만 섞고 나머지는 4지선다 객관식으로 낸다 — 단답형을 1문제로 제한하는 이유는 국어 단답형이 정답이 여럿일 여지(동의어 등)가 있어 표기 차이로 인한 오채점 위험이 수학보다 크기 때문이다
- 난이도는 학년 표준을 기본값으로 하며(학년 자체는 고정, 그 학년 안에서 쉬운/어려운 정도만 미세 조정), 부모 확인 없이 임의로 크게 바꾸지 않는다 (주간 리포트에서 최근 7일 결과를 보여주고 조정 여부를 물어본다 — 시스템이 매주 알아서 발송하는 게 아니라 부모가 원할 때 조회하는 방식)
- 힌트를 사용한 문제는 정답이어도 정답 점수의 50%만 인정한다 (힌트 사용 여부를 구분해 기록). 영어는 힌트로 정답 대신 문법 규칙만 살짝 알려주며, 국어는 정답 대신 맞춤법 규칙·낱말 뜻 힌트만 살짝 알려준다. 점수 인정 기준(사용 시 50%)은 과목과 무관하게 모두 동일하다
- 영어는 3~6학년만 지원한다 — 학년 선택 후 과목·난이도를 고르는 화면에서 1~2학년을 고른 상태면 "영어" 버튼 자체를 숨긴다. 자연어로 1~2학년+영어 조합을 요청하면 수학의 학년범위 가드레일과 같은 방식으로 정중히 거절 안내한다. 국어는 수학과 같은 전 학년(1~6) 공통 교과라 이런 학년 제외 규칙이 없다
- 영어 오답 선택지 3개는 헷갈리기 쉬운 유사 단어·문법으로 구성하고, 난이도에 따라 헷갈리는 정도를 조절한다(쉬움일수록 뻔하게 틀리고, 어려움일수록 헷갈리게). 영어의 오류 교차검증은 정답의 유일성뿐 아니라 나머지 오답 3개가 확실히 틀렸는지도 함께 판정한다. 국어의 4지선다 객관식 문제도 같은 방식(난이도별 헷갈림 조절 + 오답 3개까지 교차검증)을 쓰고, 국어의 단답형 1문제는 수학과 같이 정답 유일성만 교차검증한다(문제 자체를 답이 하나로 정해지는 유형으로만 생성해 유일성을 확보한다)
- 인쇄물로 푼 답은 자동 채점하지 않는다 (연습용으로만 취급)
- 정답 인정 범위를 별도로 두어 표기 차이로 인한 오채점을 방지한다: 단위 표기 차이(예: "5"·"5개"), 띄어쓰기 차이(예: "5 개"·"5개"), 숫자 표기 형식 차이(예: "1/2"·"0.5"), 전각/반각·대소문자 차이(예: "A"·"a", "5"·"５")를 모두 정답으로 인정한다 (이 규칙은 단답형인 수학에만 해당하며, 4지선다형인 영어는 선택지를 고르는 방식이라 표기 정규화 자체가 필요 없다). 국어의 단답형 1문제는 이 수학 규칙 대신 별도의 "핵심 단어 일치" 정규화를 쓴다 — 조사·띄어쓰기·마침표 차이는 무시하고 핵심 정답 단어가 아이 답에 포함되어 있으면(또는 그 반대로 포함되면) 정답으로 인정한다. 국어의 4지선다 객관식은 영어와 마찬가지로 표기 정규화가 필요 없다
- AI가 생성한 문제·정답은 아이에게 노출되기 전에 매 문제마다 다른 AI로 전수 교차검증하며, 오류(정오답 오류, 오타/표기 오류)가 발견되면 그 문제만 제외하고 다른 문제로 자동 교체한다 (교체 후에도 남는 오류율은 100문제당 1개(1%) 이하로 관리 — 이 "100문제"는 전체 누적이 아니라 **최근 100개 롤링 윈도우** 기준이다(최근 품질 저하가 누적치에 희석되지 않도록). 100문제가 쌓이기 전에는 비율 대신 오류 건수로 관리)
- 교체된 문제도 검증에 실패하면 최대 3회까지 재시도하고, 그래도 실패하면 부모에게 문제은행 대체 승인을 요청한다 (30초 내 응답이 없으면 자동으로 사전에 검증된 문제은행으로 대체하고 부모에게 사후 알림 — 문제은행은 학년×난이도 조합별로 준비하며, 현재는 1~6학년의 '표준' 난이도만 각 10문제씩 채워져 있고 '쉬움'·'어려움' 조합은 아직 준비되지 않음). 과목이 여러 개가 되면 부모 승인 대기 목록에 어떤 과목에서 발생한 요청인지 구분해서 보여준다
- 영어 문제은행도 같은 방식(검증 3회 실패 시 대체)을 쓴다. 3~6학년 × 표준 난이도 각 10문제(총 40문제)를 AI 생성+교차검증 파이프라인으로 먼저 채우고, '쉬움'·'어려움'은 수학과 마찬가지로 아직 준비하지 않는다. 시딩 중 교차검증에 반복 실패하는 모호한 문항은 스킵하고 다른 문제로 대체 생성한다
- 국어 문제은행도 같은 방식을 쓴다. 1~6학년 × 표준 난이도 각 10문제(총 60문제, 세트마다 단답형 1문제 포함)를 AI 생성+교차검증 파이프라인으로 먼저 채우고, '쉬움'·'어려움'은 아직 준비하지 않는다
- 한자 쓰기/서술형/영어 작문의 자동채점 제외 방침은 한문·영어·국어 확장 시에도 그대로 적용한다 (영어는 작문 대신 4지선다형 빈칸 채우기만 내고, 국어는 긴 독해 지문·서술형 없이 맞춤법·어휘·문법 위주 객관식 + 단답형 1문제만 낸다)
- 부모 화면은 아이 화면에서 토글로 전환하며, 4자리 PIN(기본값 0000)으로 접근을 막는다 (강한 인증이 아니라 아이가 우연히 들어오지 못하게 막는 정도의 장치)
- 그날 받은 학습지의 모든 문제에 답을 쓰기 전에는 새 학습지를 받을 수 없다 (하루 한 번이라는 횟수 제한은 두지 않되, 풀던 것을 다 안 쓰고 건너뛰는 것은 막는다). 과목이 여러 개(수학·영어·국어)로 늘어나면 이 게이트는 "그날 지원되는 모든 과목" 단위로 적용한다 — 이미 받은 과목을 다 풀었어도, 아직 받지 않은 다른 과목이 남아 있다면 그 과목까지 받아서 다 풀기 전에는 어떤 과목이든 새 학습지를 받을 수 없다 (하루치 완주가 서비스의 핵심 가치이기 때문 — 하루 세트 = 그날 지원되는 모든 과목의 학습지 각 1세트)
- 점수(별) 보상은 과목이 늘어나도 과목별로 나누지 않고 통합 체계 하나로 운영한다 (관리 포인트를 늘리지 않기 위해) — 단, 아래 5번 성공 기준은 별과 달리 과목별로 독립적으로 측정한다
- 점수(별) 산정 기준(구현 완료 2026-09-17, 국어도 동일 적용 2026-09-18): 문제 하나를 채점할 때마다 — 힌트 없이 정답이면 별 2개, 힌트를 보고 정답이면 별 1개, 오답이면 0개를 준다 (수학·영어·국어 공통, 과목 통합이므로 채점되는 모든 문제에 동일하게 적용). 완료 여부나 학습 스트릭(연속 일수)은 이 별 계산에 포함하지 않는다 — 오직 문제별 정답 여부·힌트 사용 여부로만 정해지는 순수 점수제다
- "오늘 딴 별 수": 채점할 때마다(문제 하나하나 채점되는 즉시) 갱신하며, 아이 화면 상단바에 날짜·요일과 같은 자리에 항상 보이게 한다. 자정이 지나면 0부터 다시 센다. 채점을 마친 직후에는 그 학습지의 점수 배너 안에도 "오늘 딴 별" 수를 별도로 한 번 더 보여준다(상단바는 상시 노출용, 점수 배너는 방금 채점한 결과와 함께 바로 확인하라는 용도로 구분)
- 누적 별 수: 오늘 딴 별 수와 별개로 DB에 전체 누적치를 저장해두고, 채점될 때마다 "누적 별 수 = 누적 별 수 + 이번에 딴 별 수"로 오늘 카운터와 동시에 갱신한다(하루 단위로 몰아서 합산하지 않음). 절대 초기화되지 않는다(스트릭이 끊기거나 날짜가 바뀌어도 그대로 유지). 아이 화면과 부모 리포트 양쪽에 모두 보여준다
- 누적 별 수 표시 방식(단위 분해): 숫자가 커지면 별 아이콘을 개수만큼 그대로 나열하는 건 비현실적이므로, 1·10·100·1000 단위로 쪼개어 "그 단위 전용 별 아이콘 × 개수"로 보여준다 (10진법 자릿수를 그대로 별 아이콘화하는 것과 같음 — 예: 누적 2,350개면 1000단위 아이콘×2, 100단위 아이콘×3, 10단위 아이콘×5, 1단위는 0개라 표시 안 함). 단위가 커질수록(1→10→100→1000) 별 아이콘이 조금씩 더 크고 반짝이게 표현한다. 값이 0개인 단위는 아예 표시하지 않는다. (누적이 9,999를 넘어가는 경우의 상위 단위 추가는 아직 정하지 않았다 — 실제로 그 정도 규모가 되면 그때 다시 정한다)
- 단위별 색(메달 등급처럼 구분): 1개 단위=동색, 10개 단위=은색, 100개 단위=금색, 1000개 단위=무지개색(그라디언트). 크기·반짝임 단계와 색 단계가 함께 올라가서 "동 → 은 → 금 → 무지개"로 갈수록 눈에 띄게 특별해 보이도록 한다
- 별 아이콘 안에 그 단위 숫자(1/10/100/1000)를 작게 적어서, 지금 보고 있는 별이 어떤 단위인지 바로 알 수 있게 한다

- **오답노트 (구현 완료 2026-09-17):** 채점 결과가 오답이면(힌트 사용 여부와 무관, `is_correct=False`인 모든 채점 — `POST /grade`와 ReAct 자연어 채점 도구 양쪽 경로 모두 포함) 그 과목의 오답노트에 한 줄을 추가한다. 항목: 과목·학년·날짜·문제 본문·보기(영어만 해당, 수학은 빈 값)·정답·아이가 쓴 답·태그(기존 `topic` 필드를 그대로 재사용 — 새 분류 체계를 따로 만들지 않는다). 자연어 채점 경로는 `attempt_id`가 없어 학년을 알 수 없으므로 그 경우 학년은 빈 값으로 남는다(문제은행·자연어 채점 경로에서 `topic`이 빈 문자열로 남는 것과 같은 종류의 한계)
  - 노트는 **과목별로 별도**이며(수학 오답노트, 영어 오답노트가 각각 독립), 한도도 **노트 하나당 최대 50개**다(과목이 2개면 도합 최대 100개까지 저장 가능, 과목이 늘어나도 과목별 한도는 그대로 50개)
  - 50개를 넘으면 그 과목 노트에서 **가장 오래된 항목 하나를 지워** 50개를 유지한다(FIFO). 나중에 같은 문제를 다시 풀어 맞혀도 오답노트 항목은 자동으로 지우지 않는다(오답노트는 "한때 틀렸다"는 이력이라, 정답을 맞혔다고 소급 삭제하지 않고 50개 한도로만 자연스럽게 밀려난다)
  - 같은 문제를 여러 번 틀리면 매번 새 줄로 추가한다(중복 제거·병합하지 않음) — 자주 틀리는 문제가 노트에 여러 번 나타나는 것 자체가 신호
  - **아이 화면에는 절대 노출하지 않는다.** 부모 화면(승인 대기 목록 화면) 상단에 "주간 리포트 보기"와 나란히 "오답노트 보기" 버튼을 두어 들어가는 새 화면으로 만든다(주간 리포트 화면 자체에 통합하지는 않되, 2026-09-18부터 리포트 화면 하단에도 같은 버튼을 하나 더 두어 리포트를 보다가 바로 넘어갈 수 있게 한다)
  - 오답노트 화면은 학습지 풀이 화면과 같은 방식으로 과목별 탭 전환 UI를 쓴다(수학 탭/영어 탭). 목록 정렬은 최근에 틀린 것부터(최신순)

## 5. 성공 기준
(아래 세 기준 모두: 정의는 확정됐지만, 실제로 값을 계산해 기록하는 계측 코드는 아직 구현되지 않았다 — 계측 구현은 이 문서와 별도인 후속 작업이다)
- 하루 분량(장당 10문제 내외, 하루 1~2장) 생성부터 채점까지 시스템 처리 시간 5분 이내 (문제 생성과 배포 전 오류 교차검증까지 포함, 아이가 푸는 시간은 중간에 자리를 비운 시간까지 포함해 모두 제외, 시스템 로그로 자동 측정하는 것을 목표로 한다, 재시도가 길어져 5분을 넘기면 4번 정책에 따라 부모 승인(최대 30초 대기 후 자동 대체)을 거쳐 문제은행에서 대체하며, 이 경우도 성공으로 인정 — 단, 문제은행이 아직 준비되지 않은 학년×난이도 조합(현재는 '쉬움'·'어려움' 대부분)을 선택해 검증이 반복 실패하는 날은 5분 이내 달성 목표에서 제외하는 예외로 처리). 영어는 오답 3개의 확실한 오답 여부까지 검증해 수학보다 검증량이 많지만, 시간 기준은 수학과 동일하게 5분으로 유지한다 (실제 구현·실측 후 초과하면 그때 재검토)
- 주 5일 이상 학습 완료(채점까지 끝난 경우만 인정, 하루는 자정 00:00 기준으로 구분)를 4주간 매주 연속으로 만족 (1차 버전 실제 출시일부터 자동으로 측정 시작)
- AI 자동생성 문제의 오류율은 4번 정책 기준을 따른다 (100문제 기준은 최근 100개 롤링 윈도우 — 4번 참고)
- 과목이 늘어나면(영어 등) 위 세 기준은 과목별로 독립적으로 측정한다 — 영어는 수학의 기존 측정에 합류하지 않고, 영어 기능이 실제 출시되는 날부터 새로 카운트를 시작한다
